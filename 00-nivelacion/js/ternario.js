const cuenta = 10;

// let mensaje;

// if(cuenta < 0)
//     mensaje = 'no hay plata bro';
// else
//     mensaje = 'tenes plata man';

// operador ternario en if y else, ? true response : false response 
const mensaje = cuenta < 0 ? 'no hay saldo' : 'hay saldo';


document.write(mensaje);